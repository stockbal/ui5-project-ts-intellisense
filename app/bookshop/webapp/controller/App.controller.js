sap.ui.define(
  ["sap/ui/core/mvc/Controller"],
  /**
   *
   * @param {typeof import('shop/bookshop/controller/BaseController').default} Controller
   */
  (Controller) => {
    "use strict";

    return Controller.extend("shop.bookshop.controller.App", { onInit() {} });
  },
);
